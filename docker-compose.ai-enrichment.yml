services:
  ai-enrichment-worker:
    build:
      context: .
      dockerfile: Dockerfile.ai-enrichment
    container_name: ai-enrichment-worker
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    command: ["python", "ai_enrichment_worker.py"]
